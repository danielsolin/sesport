@page
@model ErrorModel
@{
   ViewData["Title"] = "Fel";
}

<h1 class="text-danger">Något gick fel.</h1>
<h2 class="text-danger">
   Vi kunde inte behandla din begäran.
</h2>

@if(Model.ShowRequestId)
{
   <p>
      <strong>Begärans ID:</strong>
      <code>@Model.RequestId</code>
   </p>
}
