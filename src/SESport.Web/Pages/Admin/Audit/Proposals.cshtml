@page
@model SESport.Web.Pages.Admin.Audit.ProposalsModel
@{
   ViewData["Title"] = "Activity Proposal Audit";
}

<section class="page-heading page-heading-row">
   <div>
      <p class="eyebrow">Admin</p>
      <h1>Activity proposals</h1>
   </div>
   <a class="button" asp-page="/Admin/Index">Back</a>
</section>

@if(Model.LoadError is not null)
{
   <div class="notice notice-warning">
      @Model.LoadError
   </div>
}

<section class="form-section">
   <h2>Proposals</h2>
   <table class="admin-table">
      <thead>
         <tr>
            <th>Time</th>
            <th>Activity</th>
            <th>Producer</th>
            <th>Status</th>
            <th>Type</th>
            <th>Sport</th>
            <th></th>
         </tr>
      </thead>
      <tbody>
         @foreach(var proposal in Model.Proposals)
         {
            <tr>
               <td>@proposal.TimeText</td>
               <td>
                  <a asp-page="./Proposal" asp-route-id="@proposal.Id">
                     <strong>@proposal.Title</strong>
                  </a>
               </td>
               <td>@proposal.Producer</td>
               <td>@proposal.Status</td>
               <td>@proposal.ActivityType</td>
               <td>@proposal.Sport</td>
               <td class="table-actions">
                  <a asp-page="./Proposal" asp-route-id="@proposal.Id">
                     Review
                  </a>
               </td>
            </tr>
         }
      </tbody>
   </table>
</section>
